# Workflow Status Template

# This tracks progress through BMM methodology Analysis, Planning, and Solutioning phases.
# Implementation phase is tracked separately in sprint-status.yaml

# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - recommended: Strongly suggested but not required
#   - conditional: Required only if certain conditions met (e.g., if_has_ui)
#
# Completion Status:
#   - {file-path}: File created/found (e.g., "docs/product-brief.md")
#   - skipped: Optional/conditional workflow that was skipped

generated: "2025-11-19T08:10:00Z"
last_updated: "2025-11-19T08:45:00Z"
project: "Algorithm-Visualization-Project"
project_type: "brownfield"
selected_track: "method"
field_type: "brownfield"
workflow_path: "paths/method-brownfield.yaml"

workflow_status:
  # PREREQUISITE: Documentation (for undocumented brownfield)
  prerequisite:
    document-project: "docs/index.md"  # Completed with exhaustive scan

  # PHASE 0: Discovery (Optional - User Selected)
  discovery:
    brainstorm-project: skipped
    research: optional
    product-brief: skipped

  # PHASE 1: Planning
  planning:
    prd: docs/prd.md
    validate-prd: optional
    create-design: conditional  # if_has_ui

  # PHASE 2: Solutioning
  solutioning:
    create-architecture: recommended
    create-epics-and-stories: required
    test-design: recommended
    validate-architecture: optional
    implementation-readiness: required

  # PHASE 3: Implementation
  implementation:
    sprint-planning: required
